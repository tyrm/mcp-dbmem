---
version: '3.7'
services:
  postgres:
    image: postgres:14
    ports:
      - "5432:5432/tcp"
    environment:
      - POSTGRES_PASSWORD=mcp-pgmem
      - POSTGRES_USER=mcp-pgmem
      - POSTGRES_DB=mcp-pgmem
